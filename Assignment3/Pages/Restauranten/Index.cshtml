@page
@model Assignment3.Pages.Restauranten.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CheckInd[0].RoomNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckInd[0].NumberOfAdults)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckInd[0].NumberOfChildren)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckInd[0].Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CheckInd) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoomNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberOfAdults)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberOfChildren)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CheckIndId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CheckIndId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CheckIndId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
